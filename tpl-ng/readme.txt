These are browser side templates.

But they need to be requestable via http.

Therefore they are set up in golang http server like /css or /js.

As opposed to /template-go - which should only be available to golang serverside.